const PRICES = {

  // ── Apartment / house cleaning ──────────────────────────────────────────
  regularCleaning:  200,    // PLN  — starting from
  deepCleaning:     350,    // PLN  — starting from
  afterRenovation:  12.5,   // PLN per m²

  // ── Sofas (dry cleaning) ────────────────────────────────────────────────
  sofa2:   140,   // PLN  — 2-seater
  sofa3:   160,   // PLN  — 3-seater
  sofa4:   180,   // PLN  — 4-seater
  sofa56:  220,   // PLN  — 5–6-seater, starting from

  // ── Mattresses (one side) ───────────────────────────────────────────────
  mattressSingle: 130,   // PLN  — single
  mattressDouble: 230,   // PLN  — double

  // ── Carpets ─────────────────────────────────────────────────────────────
  carpet:    13,   // PLN per m²
  carpeting: 12,   // PLN per m²

  // ── Other furniture ─────────────────────────────────────────────────────
  armchair: 55,   // PLN
  chair:    40,   // PLN

};
